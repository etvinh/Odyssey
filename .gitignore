node_modules/
.turbo/
dist/
.expo/
.wrangler/
coverage/
*.log
.DS_Store

# Local secrets. Copy services/backend/.env.example to .env.
# .dev.vars is wrangler's default secrets filename — we don't use it, but an
# unignored one is a credentials file waiting for `git add -A`.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# Issues and specs — local working notes, per CLAUDE.md.
.scratch/

# packages/api-client generated output is COMMITTED on purpose — see ADR-0004.

# The assignment brief. Kept locally, never committed — it is the client's
# material, not ours to publish.
INSTRUCTIONS.md
